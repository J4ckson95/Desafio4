<div>
    <h1>Lista de productos ...</h1>
    {{#each productsFromFile}}
        <table>
            <p>{{this.title}}</p>
            <p>{{this.code}}</p>
            <p>{{this.price}}</p>
            <p>{{this.category}}</p>
        </table>
    {{/each}}
</div>